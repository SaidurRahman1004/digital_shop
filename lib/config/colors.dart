import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFF6C5CE7);
  static const primaryLight = Color(0xFFA29BFE);

  // Secondary
  static const secondary = Color(0xFF00CEC9);

  // Background
  static const background = Color(0xFFF8F9FD);
  static const surface = Color(0xFFFFFFFF);

  // Text
  static const textDark = Color(0xFF2D3436);
  static const textGrey = Color(0xFF636E72);

  // Status
  static const success = Color(0xFF00B894);
  static const error = Color(0xFFE17055);
}
